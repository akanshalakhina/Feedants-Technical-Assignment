# Server
PORT=3000

# MongoDB (local standalone is fine; transactions use atomic ops, no replica set needed)
MONGO_URI=mongodb://127.0.0.1:27017/feedants

# JWT – change this to a long random string in production
JWT_SECRET=feedants_super_secret_key_change_me_in_production
